# .contextignore -- source of truth for what AI agents should not read.
# Generated by ctx-ignore on 2026-07-05. Derived files: .claudeignore,
# .cursorignore, .copilotignore, .windsurfignore. Edit here, then re-derive.
#
# This repo is currently clean; most entries fence off noise it is expected
# to grow (see .gitignore). Lockfiles and build dirs are listed prospectively.
# Lockfiles are excluded from agent context only; they stay in git.

# --- Dependencies (never edit, huge) ---
node_modules/
.venv/
venv/

# --- Lockfiles (auto-generated; keep in git, skip in context) ---
package-lock.json
yarn.lock
pnpm-lock.yaml
bun.lockb
uv.lock
poetry.lock

# --- Build output / generated ---
dist/
build/
output/
__pycache__/

# --- Test artifacts ---
coverage/
.pytest_cache/

# --- Local scratch and per-user data (gitignored, not project context) ---
local/

# --- Secrets (agents must never read these) ---
.env
.env.*

# --- Logs and OS/IDE noise ---
*.log
*.swp
.DS_Store
Thumbs.db
